var newVote = document.get.getElementById('like-btn')
<% if current_user.liked? @picture %>
  $('.like-btn').addClass('liked');
<% else %>
  $('.like-btn').addClass('like-btn');
<% end %>
$('.likes-count').html("<%= @picture.get_upvotes.size %>")
