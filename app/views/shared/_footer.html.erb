<div class="footer" style="background-image: url('<%= cl_image_path 'basgd_rxyz9y', crop: :fill %>')">
  <% if user_signed_in? %>
    <%= link_to root_path, class: "size-btn" do %>
      <%= cl_image_tag "light_pshfmb", height: 40, width: 40, crop: :fill %>
    <% end %>
    <%= simple_form_for(@picture) do |f| %>
      <%= f.input :photo, label: false, input_html: { class: "hidden-file-input" }, as: :hidden %>
      <%= f.input :photo_cache, as: :hidden %>
      <%= f.submit "Save", class: "btn btn-primary spaceautour" %>
    <% end %>
    <%= cl_image_tag "camera_bsusxt", height: 40, width: 40, crop: :fill, id:"camera" %>
  <% end %>
</div>
